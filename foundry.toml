[profile.default]
src = "src"
out = "out"
libs = ["lib"]

ignore=["mev-auction-avs"]

solc_version = '0.8.25'
evm_version = "cancun"  # hard fork that enabled EIP-1153
optimizer_runs = 800
via_ir = false
ffi = true
